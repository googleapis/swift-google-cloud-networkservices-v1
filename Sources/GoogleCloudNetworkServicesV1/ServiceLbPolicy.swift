// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// ServiceLbPolicy holds global load balancing and traffic distribution
/// configuration that can be applied to a BackendService.
public struct ServiceLbPolicy: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Identifier. Name of the ServiceLbPolicy resource. It matches pattern
  /// `projects/{project}/locations/{location}/serviceLbPolicies/{service_lb_policy_name}`.
  public var name: Swift.String = Swift.String()

  /// Output only. The timestamp when this resource was created.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. The timestamp when this resource was last updated.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// Optional. Set of label tags associated with the ServiceLbPolicy resource.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Optional. A free-text description of the resource. Max length 1024
  /// characters.
  public var description: Swift.String = Swift.String()

  /// Optional. The type of load balancing algorithm to be used. The default
  /// behavior is WATERFALL_BY_REGION.
  public var loadBalancingAlgorithm: ServiceLbPolicy.LoadBalancingAlgorithm =
    ServiceLbPolicy.LoadBalancingAlgorithm()

  /// Optional. Configuration to automatically move traffic away for unhealthy
  /// IG/NEG for the associated Backend Service.
  public var autoCapacityDrain: ServiceLbPolicy.AutoCapacityDrain? = nil

  /// Optional. Configuration related to health based failover.
  public var failoverConfig: ServiceLbPolicy.FailoverConfig? = nil

  /// Optional. Configuration to provide isolation support for the associated
  /// Backend Service.
  public var isolationConfig: ServiceLbPolicy.IsolationConfig? = nil

  /// Initialize a new instance of `ServiceLbPolicy`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ServiceLbPolicy().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Option to specify if an unhealthy IG/NEG should be considered for global
  /// load balancing and traffic routing.
  public struct AutoCapacityDrain: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. If set to 'True', an unhealthy IG/NEG will be set as drained.
    /// - An IG/NEG is considered unhealthy if less than 25% of the
    /// instances/endpoints in the IG/NEG are healthy.
    /// - This option will never result in draining more than 50% of the
    /// configured IGs/NEGs for the Backend Service.
    public var enable: Swift.Bool = Swift.Bool()

    /// Initialize a new instance of `AutoCapacityDrain`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AutoCapacityDrain().with { $0.enable = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.ServiceLbPolicy.AutoCapacityDrain"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Option to specify health based failover behavior.
  /// This is not related to Network load balancer FailoverPolicy.
  public struct FailoverConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The percentage threshold that a load balancer will begin to
    /// send traffic to failover backends. If the percentage of endpoints in a
    /// MIG/NEG is smaller than this value, traffic would be sent to failover
    /// backends if possible. This field should be set to a value between 1
    /// and 99. The default value is 50 for Global external HTTP(S) load balancer
    /// (classic) and Proxyless service mesh, and 70 for others.
    public var failoverHealthThreshold: Swift.Int32 = Swift.Int32()

    /// Initialize a new instance of `FailoverConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = FailoverConfig().with { $0.failoverHealthThreshold = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.ServiceLbPolicy.FailoverConfig"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Configuration to provide isolation support for the associated Backend
  /// Service.
  public struct IsolationConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The isolation granularity of the load balancer.
    public var isolationGranularity: ServiceLbPolicy.IsolationGranularity =
      ServiceLbPolicy.IsolationGranularity()

    /// Optional. The isolation mode of the load balancer.
    public var isolationMode: ServiceLbPolicy.IsolationMode = ServiceLbPolicy.IsolationMode()

    /// Initialize a new instance of `IsolationConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = IsolationConfig().with { $0.isolationGranularity = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.ServiceLbPolicy.IsolationConfig"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The global load balancing algorithm to be used.
  public enum LoadBalancingAlgorithm: Codable, Equatable, Sendable {
    /// The type of the loadbalancing algorithm is unspecified.
    case unspecified
    /// Balance traffic across all backends across the world proportionally based
    /// on capacity.
    case sprayToWorld
    /// Direct traffic to the nearest region with endpoints and capacity before
    /// spilling over to other regions and spread the traffic from each client to
    /// all the MIGs/NEGs in a region.
    case sprayToRegion
    /// Direct traffic to the nearest region with endpoints and capacity before
    /// spilling over to other regions. All MIGs/NEGs within a region are evenly
    /// loaded but each client might not spread the traffic to all the MIGs/NEGs
    /// in the region.
    case waterfallByRegion
    /// Attempt to keep traffic in a single zone closest to the client, before
    /// spilling over to other zones.
    case waterfallByZone
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .sprayToWorld: return 3
      case .sprayToRegion: return 4
      case .waterfallByRegion: return 5
      case .waterfallByZone: return 6
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "LOAD_BALANCING_ALGORITHM_UNSPECIFIED"
      case .sprayToWorld: return "SPRAY_TO_WORLD"
      case .sprayToRegion: return "SPRAY_TO_REGION"
      case .waterfallByRegion: return "WATERFALL_BY_REGION"
      case .waterfallByZone: return "WATERFALL_BY_ZONE"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:LoadBalancingAlgorithm/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "LOAD_BALANCING_ALGORITHM_UNSPECIFIED": self = .unspecified
      case "SPRAY_TO_WORLD": self = .sprayToWorld
      case "SPRAY_TO_REGION": self = .sprayToRegion
      case "WATERFALL_BY_REGION": self = .waterfallByRegion
      case "WATERFALL_BY_ZONE": self = .waterfallByZone
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:LoadBalancingAlgorithm/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 3: self = .sprayToWorld
      case 4: self = .sprayToRegion
      case 5: self = .waterfallByRegion
      case 6: self = .waterfallByZone
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .sprayToWorld: return try container.encode(3)
      case .sprayToRegion: return try container.encode(4)
      case .waterfallByRegion: return try container.encode(5)
      case .waterfallByZone: return try container.encode(6)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The granularity of this isolation restriction.
  public enum IsolationGranularity: Codable, Equatable, Sendable {
    /// No isolation is configured for the backend service. Traffic can overflow
    /// based on the load balancing algorithm.
    case unspecified
    /// Traffic for this service will be isolated at the cloud region level.
    case region
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .region: return 1
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "ISOLATION_GRANULARITY_UNSPECIFIED"
      case .region: return "REGION"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:IsolationGranularity/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "ISOLATION_GRANULARITY_UNSPECIFIED": self = .unspecified
      case "REGION": self = .region
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:IsolationGranularity/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .region
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .region: return try container.encode(1)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The mode of this isolation restriction, defining whether clients in a given
  /// region are allowed to reach out to another region.
  public enum IsolationMode: Codable, Equatable, Sendable {
    /// No isolation mode is configured for the backend service.
    case unspecified
    /// Traffic will be sent to the nearest region.
    case nearest
    /// Traffic will fail if no serving backends are available in the same region
    /// as the load balancer.
    case strict
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .nearest: return 1
      case .strict: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "ISOLATION_MODE_UNSPECIFIED"
      case .nearest: return "NEAREST"
      case .strict: return "STRICT"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:IsolationMode/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "ISOLATION_MODE_UNSPECIFIED": self = .unspecified
      case "NEAREST": self = .nearest
      case "STRICT": self = .strict
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:IsolationMode/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .nearest
      case 2: self = .strict
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .nearest: return try container.encode(1)
      case .strict: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkservices.v1.ServiceLbPolicy"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
