// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// `WasmPlugin` is a resource representing a service executing
/// a customer-provided Wasm module.
public struct WasmPlugin: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Identifier. Name of the `WasmPlugin` resource in the following format:
  /// `projects/{project}/locations/{location}/wasmPlugins/{wasm_plugin}`.
  public var name: Swift.String = Swift.String()

  /// Output only. The timestamp when the resource was created.
  public var createTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. The timestamp when the resource was updated.
  public var updateTime: GoogleCloudWKT.Timestamp? = nil

  /// Optional. A human-readable description of the resource.
  public var description: Swift.String = Swift.String()

  /// Optional. Set of labels associated with the `WasmPlugin` resource.
  ///
  /// The format must comply with [the following
  /// requirements](/compute/docs/labeling-resources#requirements).
  public var labels: [Swift.String: Swift.String] = [:]

  /// Optional. The ID of the `WasmPluginVersion` resource that is the
  /// currently serving one. The version referred to must be a child of this
  /// `WasmPlugin` resource.
  public var mainVersionId: Swift.String = Swift.String()

  /// Optional. Specifies the logging options for the activity performed by this
  /// plugin. If logging is enabled, plugin logs are exported to
  /// Cloud Logging.
  /// Note that the settings relate to the logs generated by using
  /// logging statements in your Wasm code.
  public var logConfig: WasmPlugin.LogConfig? = nil

  /// Optional. All versions of this `WasmPlugin` resource in the key-value
  /// format. The key is the resource ID, and the value is the `VersionDetails`
  /// object.
  ///
  /// Lets you create or update a `WasmPlugin` resource and its versions in a
  /// single request. When the `main_version_id` field is not empty, it must
  /// point to one of the `VersionDetails` objects in the map.
  ///
  /// If provided in a `PATCH` request, the new versions replace the
  /// previous set. Any version omitted from the `versions` field is removed.
  /// Because the `WasmPluginVersion` resource is immutable, if a
  /// `WasmPluginVersion` resource with the same name already exists and differs,
  /// the request fails.
  ///
  /// Note: In a `GET` request, this field is populated only if the field
  /// `GetWasmPluginRequest.view` is set to `WASM_PLUGIN_VIEW_FULL`.
  public var versions: [Swift.String: WasmPlugin.VersionDetails] = [:]

  /// Output only. List of all
  /// [extensions](https://cloud.google.com/service-extensions/docs/overview)
  /// that use this `WasmPlugin` resource.
  public var usedBy: [WasmPlugin.UsedBy] = []

  /// Initialize a new instance of `WasmPlugin`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = WasmPlugin().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Details of a `WasmPluginVersion` resource to be inlined in the
  /// `WasmPlugin` resource.
  public struct VersionDetails: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Output only. The timestamp when the resource was created.
    public var createTime: GoogleCloudWKT.Timestamp? = nil

    /// Output only. The timestamp when the resource was updated.
    public var updateTime: GoogleCloudWKT.Timestamp? = nil

    /// Optional. A human-readable description of the resource.
    public var description: Swift.String = Swift.String()

    /// Optional. Set of labels associated with the `WasmPluginVersion`
    /// resource.
    public var labels: [Swift.String: Swift.String] = [:]

    /// Optional. URI of the image containing the Wasm module, stored in
    /// Artifact Registry.
    ///
    /// The URI can refer to one of the following repository formats:
    ///
    /// * Container images: the `image_uri` must point to a container that
    /// contains a single file with the name `plugin.wasm`.
    /// When a new `WasmPluginVersion` resource is created, the digest of the
    /// image is saved in the `image_digest` field.
    /// When pulling a container image from Artifact Registry, the digest value
    /// is used instead of an image tag.
    ///
    /// * Generic artifacts: the `image_uri` must be in this format:
    /// `projects/{project}/locations/{location}/repositories/{repository}/
    /// genericArtifacts/{package}:{version}`.
    /// The specified package and version must contain a file with the name
    /// `plugin.wasm`. When a new `WasmPluginVersion` resource is created, the
    /// checksum of the contents of the file is saved in the `image_digest`
    /// field.
    public var imageUri: Swift.String = Swift.String()

    /// Output only. This field holds the digest (usually checksum) value for the
    /// plugin image. The value is calculated based on the `image_uri` field. If
    /// the `image_uri` field refers to a container image, the digest value is
    /// obtained from the container image. If the `image_uri` field refers to
    /// a generic artifact, the digest value is calculated based on the
    /// contents of the file.
    public var imageDigest: Swift.String = Swift.String()

    /// Output only. This field holds the digest (usually checksum) value for the
    /// plugin configuration. The value is calculated based on the contents of
    /// `plugin_config_data` field or the image defined by the
    /// `plugin_config_uri` field.
    public var pluginConfigDigest: Swift.String = Swift.String()

    public var pluginConfigSource: OneOf_PluginConfigSource? = nil

    /// Initialize a new instance of `VersionDetails`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = VersionDetails().with { $0.pluginConfigData = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case pluginConfigData = "pluginConfigData"
      case pluginConfigUri = "pluginConfigUri"
      case createTime = "createTime"
      case updateTime = "updateTime"
      case description = "description"
      case labels = "labels"
      case imageUri = "imageUri"
      case imageDigest = "imageDigest"
      case pluginConfigDigest = "pluginConfigDigest"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.createTime = try container.decodeIfPresent(
        GoogleCloudWKT.Timestamp.self, forKey: .createTime)
      self.updateTime = try container.decodeIfPresent(
        GoogleCloudWKT.Timestamp.self, forKey: .updateTime)
      self.description = try container.decode(Swift.String.self, forKey: .description)
      self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
      self.imageUri = try container.decode(Swift.String.self, forKey: .imageUri)
      self.imageDigest = try container.decode(Swift.String.self, forKey: .imageDigest)
      self.pluginConfigDigest = try container.decode(Swift.String.self, forKey: .pluginConfigDigest)

      var pluginConfigSource: OneOf_PluginConfigSource? = nil
      let pluginConfigSourceCheckAndSet = {
        if pluginConfigSource != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'pluginConfigSource'"))
        }
        pluginConfigSource = $0
      }
      if let pluginConfigData = try container.decodeIfPresent(
        Foundation.Data.self, forKey: .pluginConfigData)
      {
        try pluginConfigSourceCheckAndSet(.pluginConfigData(pluginConfigData))
      }
      if let pluginConfigUri = try container.decodeIfPresent(
        Swift.String.self, forKey: .pluginConfigUri)
      {
        try pluginConfigSourceCheckAndSet(.pluginConfigUri(pluginConfigUri))
      }
      self.pluginConfigSource = pluginConfigSource
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.createTime, forKey: .createTime)
      try container.encode(self.updateTime, forKey: .updateTime)
      try container.encode(self.description, forKey: .description)
      try container.encode(self.labels, forKey: .labels)
      try container.encode(self.imageUri, forKey: .imageUri)
      try container.encode(self.imageDigest, forKey: .imageDigest)
      try container.encode(self.pluginConfigDigest, forKey: .pluginConfigDigest)

      if let choice = self.pluginConfigSource {
        switch choice {
        case .pluginConfigData(let value):
          try container.encode(value, forKey: .pluginConfigData)
        case .pluginConfigUri(let value):
          try container.encode(value, forKey: .pluginConfigUri)
        }
      }
    }

    public enum OneOf_PluginConfigSource: Codable, Equatable, Sendable {
      /// Configuration for the plugin.
      /// The configuration is provided to the plugin at runtime through
      /// the `ON_CONFIGURE` callback. When a new
      /// `WasmPluginVersion` version is created, the digest of the
      /// contents is saved in the `plugin_config_digest` field.
      case pluginConfigData(Foundation.Data)
      /// URI of the plugin configuration stored in the Artifact Registry.
      /// The configuration is provided to the plugin at runtime through
      /// the `ON_CONFIGURE` callback.
      ///
      /// The URI can refer to one of the following repository formats:
      ///
      /// * Container images: the `plugin_config_uri` must point to a container
      /// that contains a single file with the name `plugin.config`.
      /// When a new `WasmPluginVersion` resource is created, the digest of the
      /// image is saved in the `plugin_config_digest` field.
      /// When pulling a container image from Artifact Registry, the digest
      /// value is used instead of an image tag.
      ///
      /// * Generic artifacts: the `plugin_config_uri` must be in this format:
      /// `projects/{project}/locations/{location}/repositories/{repository}/
      /// genericArtifacts/{package}:{version}`.
      /// The specified package and version must contain a file with the name
      /// `plugin.config`. When a new `WasmPluginVersion` resource is
      /// created, the checksum of the contents of the file is saved in the
      /// `plugin_config_digest` field.
      case pluginConfigUri(Swift.String)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.WasmPlugin.VersionDetails"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Specifies the logging options for the activity performed by this
  /// plugin. If logging is enabled, plugin logs are exported to
  /// Cloud Logging.
  public struct LogConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Optional. Specifies whether to enable logging for activity by this
    /// plugin.
    ///
    /// Defaults to `false`.
    public var enable: Swift.Bool = Swift.Bool()

    /// Non-empty default. Configures the sampling rate of activity logs, where
    /// `1.0` means all logged activity is reported and `0.0` means no activity
    /// is reported. A floating point value between `0.0` and `1.0` indicates
    /// that a percentage of log messages is stored.
    ///
    /// The default value when logging is enabled is `1.0`. The value of the
    /// field must be between `0` and `1` (inclusive).
    ///
    /// This field can be specified only if logging is enabled for this plugin.
    public var sampleRate: Swift.Float = Swift.Float()

    /// Non-empty default. Specifies the lowest level of the plugin logs that are
    /// exported to Cloud Logging. This setting relates to the logs generated by
    /// using logging statements in your Wasm code.
    ///
    /// This field is can be set only if logging is enabled for the plugin.
    ///
    /// If the field is not provided when logging is enabled, it is set to
    /// `INFO` by default.
    public var minLogLevel: WasmPlugin.LogConfig.LogLevel = WasmPlugin.LogConfig.LogLevel()

    /// Initialize a new instance of `LogConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = LogConfig().with { $0.enable = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Possible values to specify the lowest level of logs to be exported to
    /// Cloud Logging.
    public enum LogLevel: Codable, Equatable, Sendable {
      /// Unspecified value. Defaults to `LogLevel.INFO`.
      case unspecified
      /// Report logs with TRACE level and above.
      case trace
      /// Report logs with DEBUG level and above.
      case debug
      /// Report logs with INFO level and above.
      case info
      /// Report logs with WARN level and above.
      case warn
      /// Report logs with ERROR level and above.
      case error
      /// Report logs with CRITICAL level only.
      case critical
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .trace: return 1
        case .debug: return 2
        case .info: return 3
        case .warn: return 4
        case .error: return 5
        case .critical: return 6
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "LOG_LEVEL_UNSPECIFIED"
        case .trace: return "TRACE"
        case .debug: return "DEBUG"
        case .info: return "INFO"
        case .warn: return "WARN"
        case .error: return "ERROR"
        case .critical: return "CRITICAL"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:LogLevel/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "LOG_LEVEL_UNSPECIFIED": self = .unspecified
        case "TRACE": self = .trace
        case "DEBUG": self = .debug
        case "INFO": self = .info
        case "WARN": self = .warn
        case "ERROR": self = .error
        case "CRITICAL": self = .critical
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:LogLevel/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .trace
        case 2: self = .debug
        case 3: self = .info
        case 4: self = .warn
        case 5: self = .error
        case 6: self = .critical
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .trace: return try container.encode(1)
        case .debug: return try container.encode(2)
        case .info: return try container.encode(3)
        case .warn: return try container.encode(4)
        case .error: return try container.encode(5)
        case .critical: return try container.encode(6)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.WasmPlugin.LogConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Defines a resource that uses the `WasmPlugin` resource.
  public struct UsedBy: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Output only. Full name of the resource
    /// https://google.aip.dev/122#full-resource-names, for example
    /// `//networkservices.googleapis.com/projects/{project}/locations/{location}/lbRouteExtensions/{extension}`
    public var name: Swift.String = Swift.String()

    /// Initialize a new instance of `UsedBy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = UsedBy().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.WasmPlugin.UsedBy"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkservices.v1.WasmPlugin"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
