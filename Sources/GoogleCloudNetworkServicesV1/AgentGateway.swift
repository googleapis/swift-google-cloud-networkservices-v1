// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// AgentGateway represents the agent gateway resource.
public struct AgentGateway: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Identifier. Name of the AgentGateway resource. It matches pattern
  /// `projects/*/locations/*/agentGateways/<agent_gateway>`.
  public var name: Swift.String = Swift.String()

  /// Output only. The timestamp when the resource was created.
  public var createTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. The timestamp when the resource was updated.
  public var updateTime: GoogleCloudWKT.Timestamp? = nil

  /// Optional. Set of label tags associated with the AgentGateway resource.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Optional. A free-text description of the resource. Max length 1024
  /// characters.
  public var description: Swift.String = Swift.String()

  /// Optional. Etag of the resource.
  /// If this is provided, it must match the server's etag. If the provided etag
  /// does not match the server's etag, the request will fail with a 409 ABORTED
  /// error.
  public var etag: Swift.String = Swift.String()

  /// Optional. Deprecated.
  @available(*, deprecated)
  public var protocols: [AgentGateway.Protocol_] = []

  /// Optional. A list of Agent registries containing the agents, MCP servers and
  /// tools governed by the Agent Gateway. Note: Currently limited to
  /// project-scoped registries Must be of format
  /// `//agentregistry.googleapis.com/projects/{project}/locations/{location}/`
  public var registries: [Swift.String] = []

  /// Optional. Network configuration for the AgentGateway.
  public var networkConfig: AgentGateway.NetworkConfig? = nil

  /// Output only. Field for populated AgentGateway card.
  public var agentGatewayCard: AgentGateway.AgentGatewayOutputCard? = nil

  /// Deployment mode of the network proxy. Exactly one of the fields in this
  /// `oneof` must be set.
  public var deploymentMode: OneOf_DeploymentMode? = nil

  /// Initialize a new instance of `AgentGateway`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = AgentGateway().with { $0.googleManaged = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case googleManaged = "googleManaged"
    case selfManaged = "selfManaged"
    case name = "name"
    case createTime = "createTime"
    case updateTime = "updateTime"
    case labels = "labels"
    case description = "description"
    case etag = "etag"
    case protocols = "protocols"
    case registries = "registries"
    case networkConfig = "networkConfig"
    case agentGatewayCard = "agentGatewayCard"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .updateTime)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.description = try container.decode(Swift.String.self, forKey: .description)
    self.etag = try container.decode(Swift.String.self, forKey: .etag)
    self.protocols = try container.decode([AgentGateway.Protocol_].self, forKey: .protocols)
    self.registries = try container.decode([Swift.String].self, forKey: .registries)
    self.networkConfig = try container.decodeIfPresent(
      AgentGateway.NetworkConfig.self, forKey: .networkConfig)
    self.agentGatewayCard = try container.decodeIfPresent(
      AgentGateway.AgentGatewayOutputCard.self, forKey: .agentGatewayCard)

    var deploymentMode: OneOf_DeploymentMode? = nil
    let deploymentModeCheckAndSet = {
      if deploymentMode != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'deploymentMode'"))
      }
      deploymentMode = $0
    }
    if let googleManaged = try container.decodeIfPresent(
      AgentGateway.GoogleManaged?.self, forKey: .googleManaged)
    {
      try deploymentModeCheckAndSet(.googleManaged(googleManaged))
    }
    if let selfManaged = try container.decodeIfPresent(
      AgentGateway.SelfManaged?.self, forKey: .selfManaged)
    {
      try deploymentModeCheckAndSet(.selfManaged(selfManaged))
    }
    self.deploymentMode = deploymentMode
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.etag, forKey: .etag)
    try container.encode(self.protocols, forKey: .protocols)
    try container.encode(self.registries, forKey: .registries)
    try container.encode(self.networkConfig, forKey: .networkConfig)
    try container.encode(self.agentGatewayCard, forKey: .agentGatewayCard)

    if let choice = self.deploymentMode {
      switch choice {
      case .googleManaged(let value):
        try container.encode(value, forKey: .googleManaged)
      case .selfManaged(let value):
        try container.encode(value, forKey: .selfManaged)
      }
    }
  }

  /// Configuration for Google Managed deployment mode.
  /// Proxy is orchestrated and managed by GoogleCloud in a tenant project.
  public struct GoogleManaged: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Optional. Operating Mode of Agent Gateway.
    public var governedAccessPath: AgentGateway.GoogleManaged.GovernedAccessPath = AgentGateway
      .GoogleManaged.GovernedAccessPath()

    /// Initialize a new instance of `GoogleManaged`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GoogleManaged().with { $0.governedAccessPath = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// GovernedAccessPath defines the type of access to protect.
    public enum GovernedAccessPath: Codable, Equatable, Sendable {
      /// Governed access path is not specified.
      case unspecified
      /// Govern agent conections to destinations.
      case agentToAnywhere
      /// Protect connection to Agent or Tool.
      case clientToAgent
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .agentToAnywhere: return 1
        case .clientToAgent: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "GOVERNED_ACCESS_PATH_UNSPECIFIED"
        case .agentToAnywhere: return "AGENT_TO_ANYWHERE"
        case .clientToAgent: return "CLIENT_TO_AGENT"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:GovernedAccessPath/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "GOVERNED_ACCESS_PATH_UNSPECIFIED": self = .unspecified
        case "AGENT_TO_ANYWHERE": self = .agentToAnywhere
        case "CLIENT_TO_AGENT": self = .clientToAgent
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:GovernedAccessPath/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .agentToAnywhere
        case 2: self = .clientToAgent
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .agentToAnywhere: return try container.encode(1)
        case .clientToAgent: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway.GoogleManaged"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Configuration for Self Managed deployment mode.
  /// Attach to existing Application Load Balancers or Secure Web Proxies.
  public struct SelfManaged: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Optional. A supported Google Cloud networking proxy in the Project and
    /// Location
    public var resourceUri: Swift.String = Swift.String()

    /// Optional. List of supported Google Cloud networking proxies in the Project and
    /// Location.
    /// resource_uris is mutually exclusive with resource_uri.
    public var resourceUris: [Swift.String] = []

    /// Initialize a new instance of `SelfManaged`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SelfManaged().with { $0.resourceUri = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway.SelfManaged"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// NetworkConfig contains network configurations for the AgentGateway.
  public struct NetworkConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Optional. Optional PSC-Interface network attachment for connectivity to
    /// your private VPCs network.
    public var egress: AgentGateway.NetworkConfig.Egress? = nil

    /// Optional. Optional DNS peering configuration for connectivity to your
    /// private VPC network.
    public var dnsPeeringConfig: AgentGateway.NetworkConfig.DnsPeeringConfig? = nil

    /// Initialize a new instance of `NetworkConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = NetworkConfig().with { $0.egress = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Configuration for Egress
    public struct Egress: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Optional. The URI of the Network Attachment resource.
      public var networkAttachment: Swift.String = Swift.String()

      /// Optional. TrustConfig defines the trust configuration for egress.
      public var trustConfig: AgentGateway.NetworkConfig.Egress.TrustConfig? = nil

      /// Initialize a new instance of `Egress`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Egress().with { $0.networkAttachment = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// TrustConfig defines the trust configuration for egress.
      public struct TrustConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Required. PEM encoded root certificates used to validate the identity
        /// of the upstream servers/destinations during egress connections.
        public var pemCertificates: [Swift.String] = []

        /// Initialize a new instance of `TrustConfig`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = TrustConfig().with { $0.pemCertificates = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway.NetworkConfig.Egress.TrustConfig"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway.NetworkConfig.Egress"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// DNS peering config for the user VPC network.
    public struct DnsPeeringConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Required. Domain names for which DNS queries should be forwarded to the
      /// target network.
      public var domains: [Swift.String] = []

      /// Required. Target project ID to which DNS queries should be forwarded
      /// to. This can be the same project that contains the AgentGateway or a
      /// different project.
      public var targetProject: Swift.String = Swift.String()

      /// Required. Target network in 'target project' to which DNS queries
      /// should be forwarded to. Must be in format of
      /// `projects/{project}/global/networks/{network}`.
      public var targetNetwork: Swift.String = Swift.String()

      /// Initialize a new instance of `DnsPeeringConfig`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = DnsPeeringConfig().with { $0.domains = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway.NetworkConfig.DnsPeeringConfig"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway.NetworkConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// AgentGatewayOutputCard contains informational output-only fields
  public struct AgentGatewayOutputCard: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Output only. mTLS Endpoint associated with this AgentGateway
    public var mtlsEndpoint: Swift.String = Swift.String()

    /// Output only. Root Certificates for Agents to validate this AgentGateway
    public var rootCertificates: [Swift.String] = []

    /// Output only. Service Account used by Service Extensions to operate.
    public var serviceExtensionsServiceAccount: Swift.String = Swift.String()

    /// Initialize a new instance of `AgentGatewayOutputCard`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AgentGatewayOutputCard().with { $0.mtlsEndpoint = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway.AgentGatewayOutputCard"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Enums of all supported protocols
  public enum Protocol_: Codable, Equatable, Sendable {
    /// Unspecified protocol.
    case unspecified
    /// Message Control Plane protocol.
    case mcp
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .mcp: return 1
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PROTOCOL_UNSPECIFIED"
      case .mcp: return "MCP"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Protocol_/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PROTOCOL_UNSPECIFIED": self = .unspecified
      case "MCP": self = .mcp
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Protocol_/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .mcp
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .mcp: return try container.encode(1)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Deployment mode of the network proxy. Exactly one of the fields in this
  /// `oneof` must be set.
  public enum OneOf_DeploymentMode: Codable, Equatable, Sendable {
    /// Optional. Proxy is orchestrated and managed by GoogleCloud in a tenant
    /// project.
    indirect case googleManaged(AgentGateway.GoogleManaged?)
    /// Optional. Attach to existing Application Load Balancers or Secure Web
    /// Proxies.
    indirect case selfManaged(AgentGateway.SelfManaged?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkservices.v1.AgentGateway"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
