// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// HttpRoute is the resource defining how HTTP traffic should be routed by a
/// Mesh or Gateway resource.
public struct HttpRoute: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Identifier. Name of the HttpRoute resource. It matches pattern
  /// `projects/*/locations/*/httpRoutes/http_route_name>`.
  public var name: Swift.String = Swift.String()

  /// Output only. Server-defined URL of this resource
  public var selfLink: Swift.String = Swift.String()

  /// Optional. A free-text description of the resource. Max length 1024
  /// characters.
  public var description: Swift.String = Swift.String()

  /// Output only. The timestamp when the resource was created.
  public var createTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. The timestamp when the resource was updated.
  public var updateTime: GoogleCloudWKT.Timestamp? = nil

  /// Required. Hostnames define a set of hosts that should match against the
  /// HTTP host header to select a HttpRoute to process the request. Hostname is
  /// the fully qualified domain name of a network host, as defined by RFC 1123
  /// with the exception that:
  ///  - IPs are not allowed.
  ///  - A hostname may be prefixed with a wildcard label (`*.`). The wildcard
  ///    label must appear by itself as the first label.
  ///
  /// Hostname can be "precise" which is a domain name without the terminating
  /// dot of a network host (e.g. `foo.example.com`) or "wildcard", which is a
  /// domain name prefixed with a single wildcard label (e.g. `*.example.com`).
  ///
  /// Note that as per RFC1035 and RFC1123, a label must consist of lower case
  /// alphanumeric characters or '-', and must start and end with an alphanumeric
  /// character. No other punctuation is allowed.
  ///
  /// The routes associated with a Mesh or Gateways  must have unique hostnames.
  /// If you attempt to attach multiple routes with conflicting hostnames,
  /// the configuration will be rejected.
  ///
  /// For example, while it is acceptable for routes for the hostnames
  /// `*.foo.bar.com` and `*.bar.com` to be associated with the same Mesh (or
  /// Gateways under the same scope), it is not possible to associate two routes
  /// both with `*.bar.com` or both with `bar.com`.
  public var hostnames: [Swift.String] = []

  /// Optional. Meshes defines a list of meshes this HttpRoute is attached to, as
  /// one of the routing rules to route the requests served by the mesh.
  ///
  /// Each mesh reference should match the pattern:
  /// `projects/*/locations/*/meshes/<mesh_name>`
  ///
  /// The attached Mesh should be of a type SIDECAR
  public var meshes: [Swift.String] = []

  /// Optional. Gateways defines a list of gateways this HttpRoute is attached
  /// to, as one of the routing rules to route the requests served by the
  /// gateway.
  ///
  /// Each gateway reference should match the pattern:
  /// `projects/*/locations/*/gateways/<gateway_name>`
  public var gateways: [Swift.String] = []

  /// Optional. Set of label tags associated with the HttpRoute resource.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Required. Rules that define how traffic is routed and handled.
  /// Rules will be matched sequentially based on the RouteMatch specified for
  /// the rule.
  public var rules: [HttpRoute.RouteRule] = []

  /// Initialize a new instance of `HttpRoute`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = HttpRoute().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Specifies how to select a route rule based on HTTP request headers.
  public struct HeaderMatch: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The name of the HTTP header to match against.
    public var header: Swift.String = Swift.String()

    /// If specified, the match result will be inverted before checking. Default
    /// value is set to false.
    public var invertMatch: Swift.Bool = Swift.Bool()

    public var matchType: OneOf_MatchType? = nil

    /// Initialize a new instance of `HeaderMatch`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = HeaderMatch().with { $0.exactMatch = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case exactMatch = "exactMatch"
      case regexMatch = "regexMatch"
      case prefixMatch = "prefixMatch"
      case presentMatch = "presentMatch"
      case suffixMatch = "suffixMatch"
      case rangeMatch = "rangeMatch"
      case header = "header"
      case invertMatch = "invertMatch"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.header = try container.decode(Swift.String.self, forKey: .header)
      self.invertMatch = try container.decode(Swift.Bool.self, forKey: .invertMatch)

      var matchType: OneOf_MatchType? = nil
      let matchTypeCheckAndSet = {
        if matchType != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'matchType'"))
        }
        matchType = $0
      }
      if let exactMatch = try container.decodeIfPresent(Swift.String.self, forKey: .exactMatch) {
        try matchTypeCheckAndSet(.exactMatch(exactMatch))
      }
      if let regexMatch = try container.decodeIfPresent(Swift.String.self, forKey: .regexMatch) {
        try matchTypeCheckAndSet(.regexMatch(regexMatch))
      }
      if let prefixMatch = try container.decodeIfPresent(Swift.String.self, forKey: .prefixMatch) {
        try matchTypeCheckAndSet(.prefixMatch(prefixMatch))
      }
      if let presentMatch = try container.decodeIfPresent(Swift.Bool.self, forKey: .presentMatch) {
        try matchTypeCheckAndSet(.presentMatch(presentMatch))
      }
      if let suffixMatch = try container.decodeIfPresent(Swift.String.self, forKey: .suffixMatch) {
        try matchTypeCheckAndSet(.suffixMatch(suffixMatch))
      }
      if let rangeMatch = try container.decodeIfPresent(
        HttpRoute.HeaderMatch.IntegerRange?.self, forKey: .rangeMatch)
      {
        try matchTypeCheckAndSet(.rangeMatch(rangeMatch))
      }
      self.matchType = matchType
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.header, forKey: .header)
      try container.encode(self.invertMatch, forKey: .invertMatch)

      if let choice = self.matchType {
        switch choice {
        case .exactMatch(let value):
          try container.encode(value, forKey: .exactMatch)
        case .regexMatch(let value):
          try container.encode(value, forKey: .regexMatch)
        case .prefixMatch(let value):
          try container.encode(value, forKey: .prefixMatch)
        case .presentMatch(let value):
          try container.encode(value, forKey: .presentMatch)
        case .suffixMatch(let value):
          try container.encode(value, forKey: .suffixMatch)
        case .rangeMatch(let value):
          try container.encode(value, forKey: .rangeMatch)
        }
      }
    }

    /// Represents an integer value range.
    public struct IntegerRange: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Start of the range (inclusive)
      public var start: Swift.Int32 = Swift.Int32()

      /// End of the range (exclusive)
      public var end: Swift.Int32 = Swift.Int32()

      /// Initialize a new instance of `IntegerRange`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = IntegerRange().with { $0.start = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.HeaderMatch.IntegerRange"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public enum OneOf_MatchType: Codable, Equatable, Sendable {
      /// The value of the header should match exactly the content of
      /// exact_match.
      case exactMatch(Swift.String)
      /// The value of the header must match the regular expression specified in
      /// regex_match. For regular expression grammar, please see:
      /// https://github.com/google/re2/wiki/Syntax
      case regexMatch(Swift.String)
      /// The value of the header must start with the contents of prefix_match.
      case prefixMatch(Swift.String)
      /// A header with header_name must exist. The match takes place whether or
      /// not the header has a value.
      case presentMatch(Swift.Bool)
      /// The value of the header must end with the contents of suffix_match.
      case suffixMatch(Swift.String)
      /// If specified, the rule will match if the request header value is within
      /// the range.
      indirect case rangeMatch(HttpRoute.HeaderMatch.IntegerRange?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.HeaderMatch"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Specifications to match a query parameter in the request.
  public struct QueryParameterMatch: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The name of the query parameter to match.
    public var queryParameter: Swift.String = Swift.String()

    public var matchType: OneOf_MatchType? = nil

    /// Initialize a new instance of `QueryParameterMatch`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = QueryParameterMatch().with { $0.exactMatch = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case exactMatch = "exactMatch"
      case regexMatch = "regexMatch"
      case presentMatch = "presentMatch"
      case queryParameter = "queryParameter"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.queryParameter = try container.decode(Swift.String.self, forKey: .queryParameter)

      var matchType: OneOf_MatchType? = nil
      let matchTypeCheckAndSet = {
        if matchType != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'matchType'"))
        }
        matchType = $0
      }
      if let exactMatch = try container.decodeIfPresent(Swift.String.self, forKey: .exactMatch) {
        try matchTypeCheckAndSet(.exactMatch(exactMatch))
      }
      if let regexMatch = try container.decodeIfPresent(Swift.String.self, forKey: .regexMatch) {
        try matchTypeCheckAndSet(.regexMatch(regexMatch))
      }
      if let presentMatch = try container.decodeIfPresent(Swift.Bool.self, forKey: .presentMatch) {
        try matchTypeCheckAndSet(.presentMatch(presentMatch))
      }
      self.matchType = matchType
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.queryParameter, forKey: .queryParameter)

      if let choice = self.matchType {
        switch choice {
        case .exactMatch(let value):
          try container.encode(value, forKey: .exactMatch)
        case .regexMatch(let value):
          try container.encode(value, forKey: .regexMatch)
        case .presentMatch(let value):
          try container.encode(value, forKey: .presentMatch)
        }
      }
    }

    public enum OneOf_MatchType: Codable, Equatable, Sendable {
      /// The value of the query parameter must exactly match the contents of
      /// exact_match.
      ///
      /// Only one of exact_match, regex_match, or present_match must be set.
      case exactMatch(Swift.String)
      /// The value of the query parameter must match the regular expression
      /// specified by regex_match. For regular expression grammar, please see
      /// https://github.com/google/re2/wiki/Syntax
      ///
      /// Only one of exact_match, regex_match, or present_match must be set.
      case regexMatch(Swift.String)
      /// Specifies that the QueryParameterMatcher matches if request contains
      /// query parameter, irrespective of whether the parameter has a value or
      /// not.
      ///
      /// Only one of exact_match, regex_match, or present_match must be set.
      case presentMatch(Swift.Bool)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.QueryParameterMatch"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// RouteMatch defines specifications used to match requests. If multiple match
  /// types are set, this RouteMatch will match if ALL type of matches are
  /// matched.
  public struct RouteMatch: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Specifies if prefix_match and full_path_match matches are case sensitive.
    /// The default value is false.
    public var ignoreCase: Swift.Bool = Swift.Bool()

    /// Specifies a list of HTTP request headers to match against. ALL of the
    /// supplied headers must be matched.
    public var headers: [HttpRoute.HeaderMatch] = []

    /// Specifies a list of query parameters to match against. ALL of the query
    /// parameters must be matched.
    public var queryParameters: [HttpRoute.QueryParameterMatch] = []

    public var pathMatch: OneOf_PathMatch? = nil

    /// Initialize a new instance of `RouteMatch`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RouteMatch().with { $0.fullPathMatch = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case fullPathMatch = "fullPathMatch"
      case prefixMatch = "prefixMatch"
      case regexMatch = "regexMatch"
      case ignoreCase = "ignoreCase"
      case headers = "headers"
      case queryParameters = "queryParameters"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.ignoreCase = try container.decode(Swift.Bool.self, forKey: .ignoreCase)
      self.headers = try container.decode([HttpRoute.HeaderMatch].self, forKey: .headers)
      self.queryParameters = try container.decode(
        [HttpRoute.QueryParameterMatch].self, forKey: .queryParameters)

      var pathMatch: OneOf_PathMatch? = nil
      let pathMatchCheckAndSet = {
        if pathMatch != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'pathMatch'"))
        }
        pathMatch = $0
      }
      if let fullPathMatch = try container.decodeIfPresent(
        Swift.String.self, forKey: .fullPathMatch)
      {
        try pathMatchCheckAndSet(.fullPathMatch(fullPathMatch))
      }
      if let prefixMatch = try container.decodeIfPresent(Swift.String.self, forKey: .prefixMatch) {
        try pathMatchCheckAndSet(.prefixMatch(prefixMatch))
      }
      if let regexMatch = try container.decodeIfPresent(Swift.String.self, forKey: .regexMatch) {
        try pathMatchCheckAndSet(.regexMatch(regexMatch))
      }
      self.pathMatch = pathMatch
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.ignoreCase, forKey: .ignoreCase)
      try container.encode(self.headers, forKey: .headers)
      try container.encode(self.queryParameters, forKey: .queryParameters)

      if let choice = self.pathMatch {
        switch choice {
        case .fullPathMatch(let value):
          try container.encode(value, forKey: .fullPathMatch)
        case .prefixMatch(let value):
          try container.encode(value, forKey: .prefixMatch)
        case .regexMatch(let value):
          try container.encode(value, forKey: .regexMatch)
        }
      }
    }

    public enum OneOf_PathMatch: Codable, Equatable, Sendable {
      /// The HTTP request path value should exactly match this value.
      ///
      /// Only one of full_path_match, prefix_match, or regex_match should be
      /// used.
      case fullPathMatch(Swift.String)
      /// The HTTP request path value must begin with specified prefix_match.
      /// prefix_match must begin with a /.
      ///
      /// Only one of full_path_match, prefix_match, or regex_match should be
      /// used.
      case prefixMatch(Swift.String)
      /// The HTTP request path value must satisfy the regular expression
      /// specified by regex_match after removing any query parameters and anchor
      /// supplied with the original URL. For regular expression grammar, please
      /// see https://github.com/google/re2/wiki/Syntax
      ///
      /// Only one of full_path_match, prefix_match, or regex_match should be
      /// used.
      case regexMatch(Swift.String)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.RouteMatch"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Specifications of a destination to which the request should be routed to.
  public struct Destination: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The URL of a BackendService to route traffic to.
    public var serviceName: Swift.String = Swift.String()

    /// Specifies the proportion of requests forwarded to the backend referenced
    /// by the serviceName field. This is computed as:
    /// - weight/Sum(weights in this destination list).
    /// For non-zero values, there may be some epsilon from the exact proportion
    /// defined here depending on the precision an implementation supports.
    ///
    /// If only one serviceName is specified and it has a weight greater than 0,
    /// 100% of the traffic is forwarded to that backend.
    ///
    /// If weights are specified for any one service name, they need to be
    /// specified for all of them.
    ///
    /// If weights are unspecified for all services, then, traffic is distributed
    /// in equal proportions to all of them.
    public var weight: Swift.Int32 = Swift.Int32()

    /// Optional. The specification for modifying the headers of a matching
    /// request prior to delivery of the request to the destination. If
    /// HeaderModifiers are set on both the Destination and the RouteAction, they
    /// will be merged. Conflicts between the two will not be resolved on the
    /// configuration.
    public var requestHeaderModifier: HttpRoute.HeaderModifier? = nil

    /// Optional. The specification for modifying the headers of a response prior
    /// to sending the response back to the client. If HeaderModifiers are set on
    /// both the Destination and the RouteAction, they will be merged. Conflicts
    /// between the two will not be resolved on the configuration.
    public var responseHeaderModifier: HttpRoute.HeaderModifier? = nil

    /// Initialize a new instance of `Destination`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Destination().with { $0.serviceName = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.Destination"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The specification for redirecting traffic.
  public struct Redirect: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The host that will be used in the redirect response instead of the one
    /// that was supplied in the request.
    public var hostRedirect: Swift.String = Swift.String()

    /// The path that will be used in the redirect response instead of the one
    /// that was supplied in the request.
    /// path_redirect can not be supplied together with prefix_redirect. Supply
    /// one alone or neither. If neither is supplied, the path of the original
    /// request will be used for the redirect.
    public var pathRedirect: Swift.String = Swift.String()

    /// Indicates that during redirection, the matched prefix (or path) should be
    /// swapped with this value. This option allows URLs be dynamically created
    /// based on the request.
    public var prefixRewrite: Swift.String = Swift.String()

    /// The HTTP Status code to use for the redirect.
    public var responseCode: HttpRoute.Redirect.ResponseCode = HttpRoute.Redirect.ResponseCode()

    /// If set to true, the URL scheme in the redirected request is set to https.
    /// If set to false, the URL scheme of the redirected request will remain the
    /// same as that of the request.
    ///
    /// The default is set to false.
    public var httpsRedirect: Swift.Bool = Swift.Bool()

    /// if set to true, any accompanying query portion of the original URL is
    /// removed prior to redirecting the request. If set to false, the query
    /// portion of the original URL is retained.
    ///
    /// The default is set to false.
    public var stripQuery: Swift.Bool = Swift.Bool()

    /// The port that will be used in the redirected request instead of the one
    /// that was supplied in the request.
    public var portRedirect: Swift.Int32 = Swift.Int32()

    /// Initialize a new instance of `Redirect`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Redirect().with { $0.hostRedirect = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Supported HTTP response code.
    public enum ResponseCode: Codable, Equatable, Sendable {
      /// Default value
      case unspecified
      /// Corresponds to 301.
      case movedPermanentlyDefault
      /// Corresponds to 302.
      case found
      /// Corresponds to 303.
      case seeOther
      /// Corresponds to 307. In this case, the request method will be retained.
      case temporaryRedirect
      /// Corresponds to 308. In this case, the request method will be retained.
      case permanentRedirect
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .movedPermanentlyDefault: return 1
        case .found: return 2
        case .seeOther: return 3
        case .temporaryRedirect: return 4
        case .permanentRedirect: return 5
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "RESPONSE_CODE_UNSPECIFIED"
        case .movedPermanentlyDefault: return "MOVED_PERMANENTLY_DEFAULT"
        case .found: return "FOUND"
        case .seeOther: return "SEE_OTHER"
        case .temporaryRedirect: return "TEMPORARY_REDIRECT"
        case .permanentRedirect: return "PERMANENT_REDIRECT"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ResponseCode/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "RESPONSE_CODE_UNSPECIFIED": self = .unspecified
        case "MOVED_PERMANENTLY_DEFAULT": self = .movedPermanentlyDefault
        case "FOUND": self = .found
        case "SEE_OTHER": self = .seeOther
        case "TEMPORARY_REDIRECT": self = .temporaryRedirect
        case "PERMANENT_REDIRECT": self = .permanentRedirect
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ResponseCode/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .movedPermanentlyDefault
        case 2: self = .found
        case 3: self = .seeOther
        case 4: self = .temporaryRedirect
        case 5: self = .permanentRedirect
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .movedPermanentlyDefault: return try container.encode(1)
        case .found: return try container.encode(2)
        case .seeOther: return try container.encode(3)
        case .temporaryRedirect: return try container.encode(4)
        case .permanentRedirect: return try container.encode(5)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.Redirect"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The specification for fault injection introduced into traffic to test the
  /// resiliency of clients to destination service failure. As part of fault
  /// injection, when clients send requests to a destination, delays can be
  /// introduced by client proxy on a percentage of requests before sending those
  /// requests to the destination service. Similarly requests can be aborted by
  /// client proxy for a percentage of requests.
  public struct FaultInjectionPolicy: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The specification for injecting delay to client requests.
    public var delay: HttpRoute.FaultInjectionPolicy.Delay? = nil

    /// The specification for aborting to client requests.
    public var abort: HttpRoute.FaultInjectionPolicy.Abort? = nil

    /// Initialize a new instance of `FaultInjectionPolicy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = FaultInjectionPolicy().with { $0.delay = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Specification of how client requests are delayed as part of fault
    /// injection before being sent to a destination.
    public struct Delay: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Specify a fixed delay before forwarding the request.
      public var fixedDelay: GoogleCloudWKT.Duration? = nil

      /// The percentage of traffic on which delay will be injected.
      ///
      /// The value must be between [0, 100]
      public var percentage: Swift.Int32 = Swift.Int32()

      /// Initialize a new instance of `Delay`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Delay().with { $0.fixedDelay = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.FaultInjectionPolicy.Delay"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Specification of how client requests are aborted as part of fault
    /// injection before being sent to a destination.
    public struct Abort: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// The HTTP status code used to abort the request.
      ///
      /// The value must be between 200 and 599 inclusive.
      public var httpStatus: Swift.Int32 = Swift.Int32()

      /// The percentage of traffic which will be aborted.
      ///
      /// The value must be between [0, 100]
      public var percentage: Swift.Int32 = Swift.Int32()

      /// Initialize a new instance of `Abort`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Abort().with { $0.httpStatus = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.FaultInjectionPolicy.Abort"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.FaultInjectionPolicy"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The specification for cookie-based stateful session affinity where the
  /// date plane supplies a “session cookie”  with the name "GSSA" which encodes
  /// a specific destination host and each request containing that cookie will
  /// be directed to that host as long as the destination host remains up and
  /// healthy.
  ///
  /// The gRPC proxyless mesh library or sidecar proxy will manage the session
  /// cookie but the client application code is responsible for copying the
  /// cookie from each RPC in the session to the next.
  public struct StatefulSessionAffinityPolicy: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. The cookie TTL value for the Set-Cookie header generated by
    /// the data plane. The lifetime of the cookie may be set to a value from 0
    /// to 86400 seconds (24 hours) inclusive.
    ///
    /// Set this to 0s to use a session cookie and disable cookie expiration.
    public var cookieTtl: GoogleCloudWKT.Duration? = nil

    /// Initialize a new instance of `StatefulSessionAffinityPolicy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = StatefulSessionAffinityPolicy().with { $0.cookieTtl = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.StatefulSessionAffinityPolicy"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The specification for modifying HTTP header in HTTP request and HTTP
  /// response.
  public struct HeaderModifier: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Completely overwrite/replace the headers with given map where key is the
    /// name of the header, value is the value of the header.
    public var `set`: [Swift.String: Swift.String] = [:]

    /// Add the headers with given map where key is the name of the header, value
    /// is the value of the header.
    public var add: [Swift.String: Swift.String] = [:]

    /// Remove headers (matching by header names) specified in the list.
    public var remove: [Swift.String] = []

    /// Initialize a new instance of `HeaderModifier`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = HeaderModifier().with { $0.`set` = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case `set` = "set"
      case add = "add"
      case remove = "remove"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.`set` = try container.decode([Swift.String: Swift.String].self, forKey: .`set`)
      self.add = try container.decode([Swift.String: Swift.String].self, forKey: .add)
      self.remove = try container.decode([Swift.String].self, forKey: .remove)
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.`set`, forKey: .`set`)
      try container.encode(self.add, forKey: .add)
      try container.encode(self.remove, forKey: .remove)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.HeaderModifier"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The specification for modifying the URL of the request, prior to forwarding
  /// the request to the destination.
  public struct URLRewrite: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Prior to forwarding the request to the selected destination, the matching
    /// portion of the requests path is replaced by this value.
    public var pathPrefixRewrite: Swift.String = Swift.String()

    /// Prior to forwarding the request to the selected destination, the requests
    /// host header is replaced by this value.
    public var hostRewrite: Swift.String = Swift.String()

    /// Initialize a new instance of `URLRewrite`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = URLRewrite().with { $0.pathPrefixRewrite = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.URLRewrite"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The specifications for retries.
  public struct RetryPolicy: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Specifies one or more conditions when this retry policy applies. Valid
    /// values are:
    ///   5xx: Proxy will attempt a retry if the destination service responds
    ///     with any 5xx response code, of if the destination service does not
    ///     respond at all, example: disconnect, reset, read timeout, connection
    ///     failure and refused streams.
    ///
    ///   gateway-error: Similar to 5xx, but only applies to response codes 502,
    ///     503, 504.
    ///
    ///   reset: Proxy will attempt a retry if the destination service does not
    ///     respond at all (disconnect/reset/read timeout)
    ///
    ///   connect-failure: Proxy will retry on failures connecting to destination
    ///     for example due to connection timeouts.
    ///
    ///   retriable-4xx: Proxy will retry fro retriable 4xx response codes.
    ///     Currently the only retriable error supported is 409.
    ///
    ///   refused-stream: Proxy will retry if the destination resets the stream
    ///     with a REFUSED_STREAM error code. This reset type indicates that it
    ///     is safe to retry.
    public var retryConditions: [Swift.String] = []

    /// Specifies the allowed number of retries. This number must be > 0. If not
    /// specified, default to 1.
    public var numRetries: Swift.Int32 = Swift.Int32()

    /// Specifies a non-zero timeout per retry attempt.
    public var perTryTimeout: GoogleCloudWKT.Duration? = nil

    /// Initialize a new instance of `RetryPolicy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RetryPolicy().with { $0.retryConditions = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.RetryPolicy"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Specifies the policy on how requests are shadowed to a separate mirrored
  /// destination service. The proxy does not wait for responses from the
  /// shadow service. Prior to sending traffic to the shadow service, the
  /// host/authority header is suffixed with -shadow.
  /// Mirroring is currently not supported for Cloud Run destinations.
  public struct RequestMirrorPolicy: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The destination the requests will be mirrored to. The weight of the
    /// destination will be ignored.
    public var destination: HttpRoute.Destination? = nil

    /// Optional. The percentage of requests to get mirrored to the desired
    /// destination.
    public var mirrorPercent: Swift.Float = Swift.Float()

    /// Initialize a new instance of `RequestMirrorPolicy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RequestMirrorPolicy().with { $0.destination = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.RequestMirrorPolicy"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The Specification for allowing client side cross-origin requests.
  public struct CorsPolicy: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Specifies the list of origins that will be allowed to do CORS requests.
    /// An origin is allowed if it matches either an item in allow_origins or
    /// an item in allow_origin_regexes.
    public var allowOrigins: [Swift.String] = []

    /// Specifies the regular expression patterns that match allowed origins. For
    /// regular expression grammar, please see
    /// https://github.com/google/re2/wiki/Syntax.
    public var allowOriginRegexes: [Swift.String] = []

    /// Specifies the content for Access-Control-Allow-Methods header.
    public var allowMethods: [Swift.String] = []

    /// Specifies the content for Access-Control-Allow-Headers header.
    public var allowHeaders: [Swift.String] = []

    /// Specifies the content for Access-Control-Expose-Headers header.
    public var exposeHeaders: [Swift.String] = []

    /// Specifies how long result of a preflight request can be cached in
    /// seconds. This translates to the Access-Control-Max-Age header.
    public var maxAge: Swift.String = Swift.String()

    /// In response to a preflight request, setting this to true indicates that
    /// the actual request can include user credentials. This translates to the
    /// Access-Control-Allow-Credentials header.
    ///
    /// Default value is false.
    public var allowCredentials: Swift.Bool = Swift.Bool()

    /// If true, the CORS policy is disabled. The default value is false, which
    /// indicates that the CORS policy is in effect.
    public var disabled: Swift.Bool = Swift.Bool()

    /// Initialize a new instance of `CorsPolicy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CorsPolicy().with { $0.allowOrigins = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.CorsPolicy"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Static HTTP response object to be returned.
  public struct HttpDirectResponse: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. Status to return as part of HTTP Response. Must be a positive
    /// integer.
    public var status: Swift.Int32 = Swift.Int32()

    /// Body to return as part of HTTP Response.
    public var httpBody: OneOf_HttpBody? = nil

    /// Initialize a new instance of `HttpDirectResponse`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = HttpDirectResponse().with { $0.stringBody = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case stringBody = "stringBody"
      case bytesBody = "bytesBody"
      case status = "status"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.status = try container.decode(Swift.Int32.self, forKey: .status)

      var httpBody: OneOf_HttpBody? = nil
      let httpBodyCheckAndSet = {
        if httpBody != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'httpBody'"))
        }
        httpBody = $0
      }
      if let stringBody = try container.decodeIfPresent(Swift.String.self, forKey: .stringBody) {
        try httpBodyCheckAndSet(.stringBody(stringBody))
      }
      if let bytesBody = try container.decodeIfPresent(Foundation.Data.self, forKey: .bytesBody) {
        try httpBodyCheckAndSet(.bytesBody(bytesBody))
      }
      self.httpBody = httpBody
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.status, forKey: .status)

      if let choice = self.httpBody {
        switch choice {
        case .stringBody(let value):
          try container.encode(value, forKey: .stringBody)
        case .bytesBody(let value):
          try container.encode(value, forKey: .bytesBody)
        }
      }
    }

    /// Body to return as part of HTTP Response.
    public enum OneOf_HttpBody: Codable, Equatable, Sendable {
      /// Optional. Response body as a string. Maximum body length is 1024
      /// characters.
      case stringBody(Swift.String)
      /// Optional. Response body as bytes. Maximum body size is 4096B.
      case bytesBody(Foundation.Data)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.HttpDirectResponse"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The specifications for routing traffic and applying associated policies.
  public struct RouteAction: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The destination to which traffic should be forwarded.
    public var destinations: [HttpRoute.Destination] = []

    /// If set, the request is directed as configured by this field.
    public var redirect: HttpRoute.Redirect? = nil

    /// The specification for fault injection introduced into traffic to test the
    /// resiliency of clients to backend service failure. As part of fault
    /// injection, when clients send requests to a backend service, delays can be
    /// introduced  on a percentage of requests before sending those requests to
    /// the backend service. Similarly requests from clients can be aborted for a
    /// percentage of requests.
    ///
    /// timeout and retry_policy will be ignored by clients that are configured
    /// with a fault_injection_policy
    public var faultInjectionPolicy: HttpRoute.FaultInjectionPolicy? = nil

    /// The specification for modifying the headers of a matching request prior
    /// to delivery of the request to the destination. If HeaderModifiers are set
    /// on both the Destination and the RouteAction, they will be merged.
    /// Conflicts between the two will not be resolved on the configuration.
    public var requestHeaderModifier: HttpRoute.HeaderModifier? = nil

    /// The specification for modifying the headers of a response prior to
    /// sending the response back to the client. If HeaderModifiers are set
    /// on both the Destination and the RouteAction, they will be merged.
    /// Conflicts between the two will not be resolved on the configuration.
    public var responseHeaderModifier: HttpRoute.HeaderModifier? = nil

    /// The specification for rewrite URL before forwarding requests to the
    /// destination.
    public var urlRewrite: HttpRoute.URLRewrite? = nil

    /// Specifies the timeout for selected route. Timeout is computed from the
    /// time the request has been fully processed (i.e. end of stream) up until
    /// the response has been completely processed. Timeout includes all retries.
    public var timeout: GoogleCloudWKT.Duration? = nil

    /// Specifies the retry policy associated with this route.
    public var retryPolicy: HttpRoute.RetryPolicy? = nil

    /// Specifies the policy on how requests intended for the routes destination
    /// are shadowed to a separate mirrored destination. Proxy will not wait for
    /// the shadow destination to respond before returning the response. Prior to
    /// sending traffic to the shadow service, the host/authority header is
    /// suffixed with -shadow.
    public var requestMirrorPolicy: HttpRoute.RequestMirrorPolicy? = nil

    /// The specification for allowing client side cross-origin requests.
    public var corsPolicy: HttpRoute.CorsPolicy? = nil

    /// Optional. Specifies cookie-based stateful session affinity.
    public var statefulSessionAffinity: HttpRoute.StatefulSessionAffinityPolicy? = nil

    /// Optional. Static HTTP Response object to be returned regardless of the
    /// request.
    public var directResponse: HttpRoute.HttpDirectResponse? = nil

    /// Optional. Specifies the idle timeout for the selected route. The idle
    /// timeout is defined as the period in which there are no bytes sent or
    /// received on either the upstream or downstream connection. If not set, the
    /// default idle timeout is 1 hour. If set to 0s, the timeout will be
    /// disabled.
    public var idleTimeout: GoogleCloudWKT.Duration? = nil

    /// Initialize a new instance of `RouteAction`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RouteAction().with { $0.destinations = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.RouteAction"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Specifies how to match traffic and how to route traffic when traffic is
  /// matched.
  public struct RouteRule: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// A list of matches define conditions used for matching the rule against
    /// incoming HTTP requests. Each match is independent, i.e. this rule will be
    /// matched if ANY one of the matches is satisfied.
    ///
    /// If no matches field is specified, this rule will unconditionally match
    /// traffic.
    ///
    /// If a default rule is desired to be configured, add a rule with no matches
    /// specified to the end of the rules list.
    public var matches: [HttpRoute.RouteMatch] = []

    /// The detailed rule defining how to route matched traffic.
    public var action: HttpRoute.RouteAction? = nil

    /// Initialize a new instance of `RouteRule`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RouteRule().with { $0.matches = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute.RouteRule"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkservices.v1.HttpRoute"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
