// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation

/// The part of the request or response for which the extension is called.
public enum EventType: Codable, Equatable, Sendable {
  /// Unspecified value. Do not use.
  case unspecified
  /// If included in `supported_events`,
  /// the extension is called when the HTTP request headers arrive.
  case requestHeaders
  /// If included in `supported_events`,
  /// the extension is called when the HTTP request body arrives.
  case requestBody
  /// If included in `supported_events`,
  /// the extension is called when the HTTP response headers arrive.
  case responseHeaders
  /// If included in `supported_events`,
  /// the extension is called when the HTTP response body arrives.
  case responseBody
  /// If included in `supported_events`,
  /// the extension is called when the HTTP request trailers arrives.
  case requestTrailers
  /// If included in `supported_events`,
  /// the extension is called when the HTTP response trailers arrives.
  case responseTrailers
  /// Encodes an unknown integer value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownIntValue(Int)
  /// Encodes an unknown string value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownStringValue(String)

  public init() {
    self = .unspecified
  }

  /// Returns the integer value associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown string value, this returns `nil`.
  public var intValue: Int? {
    switch self {
    case .unspecified: return 0
    case .requestHeaders: return 1
    case .requestBody: return 2
    case .responseHeaders: return 3
    case .responseBody: return 4
    case .requestTrailers: return 5
    case .responseTrailers: return 6
    case .unknownIntValue(let v): return v
    case .unknownStringValue: return nil
    }
  }

  /// Returns the string value (or name) associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
  public var stringValue: Swift.String? {
    switch self {
    case .unspecified: return "EVENT_TYPE_UNSPECIFIED"
    case .requestHeaders: return "REQUEST_HEADERS"
    case .requestBody: return "REQUEST_BODY"
    case .responseHeaders: return "RESPONSE_HEADERS"
    case .responseBody: return "RESPONSE_BODY"
    case .requestTrailers: return "REQUEST_TRAILERS"
    case .responseTrailers: return "RESPONSE_TRAILERS"
    case .unknownIntValue: return nil
    case .unknownStringValue(let v): return v
    }
  }

  /// Initialize from a string value.
  ///
  /// If the value is unknown, this initializes to [`unknownStringValue`](doc:EventType/unknownStringValue(_:)).
  public init(stringValue: Swift.String) {
    switch stringValue {
    case "EVENT_TYPE_UNSPECIFIED": self = .unspecified
    case "REQUEST_HEADERS": self = .requestHeaders
    case "REQUEST_BODY": self = .requestBody
    case "RESPONSE_HEADERS": self = .responseHeaders
    case "RESPONSE_BODY": self = .responseBody
    case "REQUEST_TRAILERS": self = .requestTrailers
    case "RESPONSE_TRAILERS": self = .responseTrailers
    default: self = .unknownStringValue(stringValue)
    }
  }

  /// Initialize from an integer value.
  ///
  /// If the value is unknown, this initializes to [`unknownIntValue`](doc:EventType/unknownIntValue(_:)).
  public init(intValue: Int) {
    switch intValue {
    case 0: self = .unspecified
    case 1: self = .requestHeaders
    case 2: self = .requestBody
    case 3: self = .responseHeaders
    case 4: self = .responseBody
    case 5: self = .requestTrailers
    case 6: self = .responseTrailers
    default: self = .unknownIntValue(intValue)
    }
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.singleValueContainer()
    if let v = try? container.decode(Int.self) {
      self.init(intValue: v)
      return
    }
    if let s = try? container.decode(String.self) {
      if let v = Int(s) {
        self.init(intValue: v)
      } else {
        self.init(stringValue: s)
      }
      return
    }
    throw DecodingError.dataCorruptedError(
      in: container, debugDescription: "Expected enum value, must be integer or string.")
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.singleValueContainer()
    switch self {
    case .unspecified: return try container.encode(0)
    case .requestHeaders: return try container.encode(1)
    case .requestBody: return try container.encode(2)
    case .responseHeaders: return try container.encode(3)
    case .responseBody: return try container.encode(4)
    case .requestTrailers: return try container.encode(5)
    case .responseTrailers: return try container.encode(6)
    case .unknownIntValue(let v): return try container.encode(v)
    case .unknownStringValue(let v): return try container.encode(v)
    }
  }
}
